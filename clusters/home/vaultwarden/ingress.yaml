apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vaultwarden
spec:
  hostnames:
  - vaultwarden.k8s.home.blrobinson.uk
  parentRefs:
  - name: gateway-ingress
    namespace: ingress
    sectionName: https
  rules:
  - backendRefs:
    - name: vaultwarden
      port: 80
